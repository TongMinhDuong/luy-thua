#include <stdio.h>
#include <stdlib.h>

int main()
{
	int a;
	int b;
	
	printf(" tinh b khi a = ", a);
	scanf("%d", &a);
	b= pow(a,a);
	printf(" khi a = %d thi b = %d", a,b);
	return 0;
}
